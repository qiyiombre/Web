{
  "name": "nebula-insight",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "description": "星云洞察：个人日志知识图谱与行为分析系统",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "check": "npm --prefix client run build && npm --prefix server test && node --check server/src/index.js",
    "migrate": "node scripts/apply-migrations.mjs",
    "start": "npm --prefix server start",
    "seed:demo": "node scripts/seed-demo-logs.mjs",
    "test": "npm --prefix server test"
  }
}
